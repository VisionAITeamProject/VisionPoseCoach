# POCO SO-101 모니터암 제어 UI

`monitor_arm_ui.py`는 4개의 STS3215 모터를 다음 순서로 사용합니다.

| 모터 ID | 조인트 | 역할 |
|---|---|---|
| 1 | `shoulder_lift` | 높이·전후 위치 IK |
| 2 | `elbow_flex` | 높이·전후 위치 IK |
| 3 | `wrist_roll` | 모니터 좌우 수평 자세 |
| 4 | `wrist_flex` | 팔 각도에 대한 pitch 짐벌 보상 |

기본 고정 높이는 `so101_new_calib.urdf` 중립 자세의 **gripper 모터 출력축 높이 0.255461m**입니다. IK는 프레임 외형 길이가 아니라 URDF의 모터 회전축 사이 변환을 사용합니다.

- `shoulder_lift → elbow_flex`: 약 0.116m
- `elbow_flex → wrist_flex`: 약 0.135m
- `wrist_flex → gripper 모터 출력축`: 중립 자세의 X/Z 평면 기준 약 0.0871m
- 마지막 구간은 `wrist_roll`과 모터의 편심 위치까지 포함하므로 roll 및 pitch에 따라 X/Z 오프셋을 다시 계산합니다.

## 안전한 시뮬레이션 실행

```bash
python monitor_arm_ui.py
```

이 모드에서는 실제 시리얼 포트나 모터를 건드리지 않습니다. `연결`을 누른 뒤 `현재 목표 명령 전송`을 누르면 계산된 엔코더 tick만 UI 상태창에 표시됩니다.

## 실제 모터 실행

모니터를 장착하기 전에 무부하로 방향, 영점, ID 매핑을 확인하십시오.

```bash
python monitor_arm_ui.py --hardware --port /dev/ttyACM0
```

실제 모드에서도 프로그램 시작만으로 모터가 움직이지 않습니다. `연결`을 눌러 ID 1~4의 응답을 확인하고, 별도의 확인창을 거쳐 명령을 전송합니다.

캘리브레이션 전에는 `0rad = 2048 tick`, 모든 모터 방향 `+1`을 임시 기본값으로 사용합니다. 실제 조립 영점은 UI에서 캘리브레이션하면 `monitor_arm_calibration.json`에 저장되고 다음 실행부터 자동 적용됩니다.

## ㄱ자 자세 영점 캘리브레이션

한 번의 기준 자세만으로는 모터 회전 방향을 자동 판별할 수 없습니다. 먼저 UI의 `ID n 방향 반전` 체크박스를 실제 조립 방향에 맞게 선택해야 합니다. 방향을 모르면 모니터를 제거한 상태에서 작은 각도 명령으로 축별 방향을 확인하십시오.

1. 실제 하드웨어 모드로 실행하고 `연결`을 누릅니다.
2. 모니터를 제거하거나 팔 전체를 확실히 지지합니다.
3. `1. 캘리브레이션 시작(토크 해제)`를 누릅니다.
4. 상부 링크를 지면에 수직으로, 하부 링크를 지면에 수평으로 맞춰 정확한 ㄱ자를 만듭니다.
5. 손목/모니터 마운트도 중립 수평 자세로 맞춥니다.
6. `2. 현재 ㄱ자 자세 저장`을 누릅니다.

새 캘리브레이션 URDF에서 출력된 프레임의 물리적 ㄱ자 자세는 다음 관절각에 해당합니다.

- `shoulder_lift`: 0°
- `elbow_flex`: 0°
- `wrist_roll`: 0°
- `wrist_flex`: 0°

모터 축 중심을 연결한 선은 모터 결합 오프셋 때문에 프레임 방향과 약 14° 차이가 나지만, 이는 IK의 축간 벡터에서 별도로 반영됩니다. 캘리브레이션은 눈으로 맞출 수 있는 출력 프레임의 수직·수평을 기준으로 합니다. 코드는 이 자세의 raw encoder tick을 각 모터의 0° 조립 영점으로 저장합니다. 저장 전에 현재 위치를 목표 위치로 기록하고 토크를 복귀시켜 갑작스러운 이동을 막습니다.

## UI 거리의 의미

- `사용자 얼굴–베이스 추정 거리`: 현재는 센서 대신 수동 입력하는 얼굴과 로봇 베이스 사이의 X축 거리입니다.
- `목표 사용자–모니터 거리`: 유지할 얼굴과 모니터 기준점 사이 거리입니다.
- gripper 모터 축의 IK 목표 X = 얼굴–베이스 거리 - 목표 사용자–모니터 거리

향후 적외선/MediaPipe 모듈은 첫 번째 값을 실시간으로 갱신하면 됩니다. 현재 IK 기준점은 URDF의 gripper 모터 출력축입니다. 실제 모니터 화면면이 이 축에서 떨어져 있다면 최종적으로 축–화면면 오프셋을 추가 측정하여 보정해야 합니다.

## 테스트

```bash
python -m unittest -v test_monitor_arm_ui.py
```
